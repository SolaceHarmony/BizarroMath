"""
Tests for the BizarroMath library.

We usually keep this empty or minimal, just so Python sees 'tests' as a package.
If you're using pytest, it doesn't even strictly require this file.
"""
# Nothing to see here! Move along, move along...